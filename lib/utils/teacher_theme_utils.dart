import 'package:flutter/material.dart';

class TeacherThemeUtils {
  // Softer teacher palette (sky → deep) to reduce eye strain vs navy.
  static const Color primary = Color(0xFF0EA5E9); // sky-500
  static const Color mid = Color(0xFF0369A1); // sky-700
  static const Color dark = Color(0xFF0C4A6E); // sky-900

  static const List<Color> chromeGradient = <Color>[dark, primary];
}

